# docs/

Project documentation, built with MkDocs.

- `architecture/` — Architecture Decision Records (ADRs). One file per major decision
  (e.g. "why edge-first federated topology", "why MQTT over HTTP for node comms").
- `api/` — Auto-generated OpenAPI docs from the backend.
- `user-guide/` — End-user setup/operation docs.
