# scripts/

Automation for setup, deployment, and day-to-day development.

- `setup/` — First-time environment bootstrap (Pi provisioning, repo clone, .env setup).
- `deploy/` — Deployment automation to a greenhouse Pi.
- `dev/` — Common dev shortcuts (reset DB, tail logs, restart a single service).
